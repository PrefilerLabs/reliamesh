{
  "displayName": "ReliaMesh API server errors",
  "combiner": "OR",
  "enabled": true,
  "documentation": {
    "content": "Five or more API server errors in five minutes. Inspect Cloud Run revision and fixed request IDs in project reliamesh; do not log payloads. See docs/operations.md. This measures service operation, not agent reliability.",
    "mimeType": "text/markdown"
  },
  "conditions": [{
    "displayName": "At least five 5xx responses in five minutes",
    "conditionThreshold": {
      "filter": "resource.type=\"cloud_run_revision\" AND resource.labels.service_name=\"reliamesh-api\" AND metric.type=\"run.googleapis.com/request_count\" AND metric.labels.response_code_class=\"5xx\"",
      "aggregations": [{"alignmentPeriod": "300s", "perSeriesAligner": "ALIGN_SUM", "crossSeriesReducer": "REDUCE_SUM"}],
      "comparison": "COMPARISON_GT",
      "thresholdValue": 4,
      "duration": "0s",
      "trigger": {"count": 1}
    }
  }],
  "alertStrategy": {"autoClose": "3600s"}
}
